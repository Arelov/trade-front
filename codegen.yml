generates:
  ./vueApollo.ts:
    schema: "http://localhost:4000/graphql"
    documents: ./src/generated/v/*.gql
    plugins:
      - typescript
      - typescript-operations
      - typescript-vue-apollo
